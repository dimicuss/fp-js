import { createStyles } from '@/lib/createStyles'


export const styles = createStyles({
    router: () => ({
        display: 'flex',
    }),
    sidebar: () => ({
        width: 200,
    }),
    page: () => ({
        padding: 10,
        width: '100%',
    }),
});