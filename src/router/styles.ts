import { createStyles } from '@/lib/createStyles'


export const styles = createStyles({
    router: () => ({
        display: 'flex',
        minHeight: '100vh',
        backgroundColor: '#202831'
    }),
    page: () => ({
        padding: '20px 40px',
        width: '100%',
    }),
});